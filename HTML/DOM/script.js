/* 
const element = document.getElementById('title')
Retorna um element 

const element = document.getElementsByClassName('one')
Retorna HTMLCollection

const element = document.getElementsByTagName('h1')
Retorna HTMLCollection

const element = document.querySelector('.one')
Retorna um element

const element = document.querySelectorAll('h1')
Retorna Nodelist -podendo usar Foreach-

console.log(element)
*/

//const element = document.querySelector('h1')

//element.textContent = "Seja bem vindo(a)"
//element.innerText = "Como vai?"
//element.innerHTML = "Espero que goste<i>!</i>"

// const element = document.querySelector('input')

// element.value = "Finalizado"

// const header = document.querySelector('header')
// header.setAttribute('id', 'header')

// const headerID = document.querySelector('#header')

// console.log(headerID.getAttribute('id'))

// const element = document.querySelector('body')

// element.style.backgroundColor = "#FF0000"
// console.log(element.style.backgroundColor)

// const element = document.querySelector('body')

// element.classList.add('active', 'blue')
// console.log(element.classList)

// element.classList.toggle('active') //ativa ou dasativa 

// const element = document.querySelector('h1')

// console.log(element.parentNode)